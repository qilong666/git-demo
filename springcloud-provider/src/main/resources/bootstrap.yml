


server:
  port: 8762

spring:
  application:
    name: springcloud-news-provider


  cloud:
    config:
      lable: master
      profile: dev
      discovery:
        enable: true
        service-id: springcloud-config
      uri: http://localhost:8088

